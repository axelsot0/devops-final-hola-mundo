# ─────────────────────────────────────────────────────────────
# Base de datos PostgreSQL
# ─────────────────────────────────────────────────────────────

# Opción A · Docker (docker compose up -d): funciona tal cual.
DATABASE_URL="postgresql://planea:planea@localhost:5432/planea_dev"

# Opción B · Supabase
# En el panel del proyecto pulsa «Connect» (arriba) y copia una cadena.
# Supabase ofrece varias; usa la de **Session pooler**, que llega por el
# puerto 5432 y su host contiene «pooler.supabase.com». Reemplaza
# [YOUR-PASSWORD] por la contraseña de la base:
#
# DATABASE_URL="postgresql://postgres.abcdefgh:TU-CLAVE@aws-0-us-east-1.pooler.supabase.com:5432/postgres"
#
# NO uses la cadena del «Transaction pooler» (puerto 6543) para preparar la
# base: usa sentencias preparadas de forma incompatible con `prisma db push`
# y el comando falla. Si aun así la necesitas para producción, añádele
# `?pgbouncer=true&connection_limit=1`.
#
# La «Direct connection» (host db.xxxx.supabase.co) también sirve, pero en
# proyectos nuevos solo responde por IPv6 y muchas redes domésticas no la
# alcanzan; por eso el Session pooler es la opción segura.

# ─────────────────────────────────────────────────────────────
# Autenticación
# ─────────────────────────────────────────────────────────────

# Secreto para firmar las sesiones de Auth.js.
# Genera uno nuevo con: npx auth secret   (o: openssl rand -base64 32)
AUTH_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"

# Necesario en desarrollo local y en despliegues detrás de un proxy.
AUTH_TRUST_HOST=true

# ─────────────────────────────────────────────────────────────
# Cifrado de secretos guardados
# ─────────────────────────────────────────────────────────────

# Clave AES-256 (32 bytes en base64) con la que se cifran los refresh tokens
# de Gmail antes de guardarlos. Si la pierdes, todos los usuarios tendrán que
# volver a conectar su correo.
# Genera una con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=""

# ─────────────────────────────────────────────────────────────
# Gmail
# ─────────────────────────────────────────────────────────────
#
# Credenciales de la app en Google Cloud. Son las mismas para toda la
# instalación: cada usuario autoriza su propio buzón desde /cuentas y su
# refresh token se guarda cifrado en la base de datos.
#
# 1. En Google Cloud Console crea un proyecto y activa la API de Gmail.
# 2. Pantalla de consentimiento: tipo "External", modo "Testing", y añade
#    como usuarios de prueba los correos de quienes vayan a probar.
# 3. Credenciales > ID de cliente de OAuth > "Aplicación web". Registra como
#    URI de redirección autorizado:
#      http://localhost:3000/oauth2callback         (desarrollo)
#      https://TU-DOMINIO/oauth2callback            (despliegue)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Opcionales.
# Fuerza el redirect URI cuando la app va detrás de un proxy que reescribe
# el host; si no, se deduce del origen de la petición.
# GOOGLE_REDIRECT_URI="https://TU-DOMINIO/oauth2callback"
# Correos que revisa cada sincronización (por defecto 300, máximo 2000).
# GMAIL_MAX_RESULTS=300
